# Python
.venv/
__pycache__/
*.pyc

# RAG runtime DB (regenerated by index_project_code)
netsdr_rag.db
netsdr_rag.db-wal
netsdr_rag.db-shm

# Build output
build/
out/
cmake-build-*/

# Compiler artifacts (MSVC/clang-cl). Never commit these; ad-hoc probe
# compilations used to dump them into the repo root.
*.obj
*.exe
*.pdb
*.ilk
*.idb
*.iobj
*.ipdb

# Editor / tooling
.vscode/
.idea/
node_modules/
# clangd compile-db symlink -> build/win-msvc/compile_commands.json (machine-local)
compile_commands.json

# Logs / runtime
*.log
